import { Button } from "@/components/ui/button";
import { MapPin, Recycle } from "lucide-react";

export default function Mapsrecicle() {
  const handleOpenAbree = () => {
    window.open("https://abree.org.br/pontos-de-recebimento", "_blank");
  };

  const primaryGreenBg = "bg-green-600 hover:bg-green-700";

  return (
    <div className="flex justify-center w-full mb-10">
      <div className="bg-green-50 border border-green-200 rounded-2xl shadow-md p-8 w-[90%] md:w-[75%] text-center">
        <div className="flex flex-col items-center gap-3">
          <Recycle className="text-green-600 w-10 h-10 mb-2" />
          <h2 className="text-2xl font-bold text-green-700">
            Encontre um ponto de coleta próximo!
          </h2>
          <p className="text-gray-700 max-w-2xl">
            A <span className="font-semibold">ABREE</span> ajuda a localizar
            locais de descarte de eletrônicos em todo o Brasil. Contribua para
            um futuro mais sustentável e dê o destino correto aos seus
            dispositivos.
          </p>

          <Button
            type="button"
            size="lg"
            onClick={handleOpenAbree}
            className={`${primaryGreenBg} text-white font-semibold px-8 py-6 rounded-xl shadow-md border border-green-700/40 transition-all duration-300 hover:scale-105 mt-4`}
          >
            <MapPin className="mr-2 h-5 w-5 text-white" />
            Pontos de Recebimento
          </Button>
        </div>
      </div>
    </div>
  );
}
